{
  "name": "cooking-app-server",
  "version": "0.0.1",
  "license": "ISC",
  "scripts": {
    "start": "json-server --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "slugid": "^3.0.0"
  }
}
